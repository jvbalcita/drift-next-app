// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: drift/v1/grid_preview.proto

package driftv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	v1 "drift.local/drift-next/gen/go/drift/v1"
	errors "errors"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// GridPreviewServiceName is the fully-qualified name of the GridPreviewService service.
	GridPreviewServiceName = "drift.v1.GridPreviewService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// GridPreviewServiceSyncGridPreviewsProcedure is the fully-qualified name of the
	// GridPreviewService's SyncGridPreviews RPC.
	GridPreviewServiceSyncGridPreviewsProcedure = "/drift.v1.GridPreviewService/SyncGridPreviews"
	// GridPreviewServiceStopGridPreviewsProcedure is the fully-qualified name of the
	// GridPreviewService's StopGridPreviews RPC.
	GridPreviewServiceStopGridPreviewsProcedure = "/drift.v1.GridPreviewService/StopGridPreviews"
)

// GridPreviewServiceClient is a client for the drift.v1.GridPreviewService service.
type GridPreviewServiceClient interface {
	// SyncGridPreviews reconciles the plane's capture set with the devices the
	// console is drawing and answers with the still held for each. It is the whole
	// of the grid's traffic: the plane owns the cadence and this call reads what
	// that cadence produced, so a console that polls it does not drive a capture
	// per device and cannot fan out.
	SyncGridPreviews(context.Context, *connect.Request[v1.SyncGridPreviewsRequest]) (*connect.Response[v1.SyncGridPreviewsResponse], error)
	// StopGridPreviews releases the capture set. A device nothing is showing stops
	// being captured, which is the same rule the one-shot capture and the live
	// mirror already obey.
	StopGridPreviews(context.Context, *connect.Request[v1.StopGridPreviewsRequest]) (*connect.Response[v1.StopGridPreviewsResponse], error)
}

// NewGridPreviewServiceClient constructs a client for the drift.v1.GridPreviewService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewGridPreviewServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) GridPreviewServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	gridPreviewServiceMethods := v1.File_drift_v1_grid_preview_proto.Services().ByName("GridPreviewService").Methods()
	return &gridPreviewServiceClient{
		syncGridPreviews: connect.NewClient[v1.SyncGridPreviewsRequest, v1.SyncGridPreviewsResponse](
			httpClient,
			baseURL+GridPreviewServiceSyncGridPreviewsProcedure,
			connect.WithSchema(gridPreviewServiceMethods.ByName("SyncGridPreviews")),
			connect.WithClientOptions(opts...),
		),
		stopGridPreviews: connect.NewClient[v1.StopGridPreviewsRequest, v1.StopGridPreviewsResponse](
			httpClient,
			baseURL+GridPreviewServiceStopGridPreviewsProcedure,
			connect.WithSchema(gridPreviewServiceMethods.ByName("StopGridPreviews")),
			connect.WithClientOptions(opts...),
		),
	}
}

// gridPreviewServiceClient implements GridPreviewServiceClient.
type gridPreviewServiceClient struct {
	syncGridPreviews *connect.Client[v1.SyncGridPreviewsRequest, v1.SyncGridPreviewsResponse]
	stopGridPreviews *connect.Client[v1.StopGridPreviewsRequest, v1.StopGridPreviewsResponse]
}

// SyncGridPreviews calls drift.v1.GridPreviewService.SyncGridPreviews.
func (c *gridPreviewServiceClient) SyncGridPreviews(ctx context.Context, req *connect.Request[v1.SyncGridPreviewsRequest]) (*connect.Response[v1.SyncGridPreviewsResponse], error) {
	return c.syncGridPreviews.CallUnary(ctx, req)
}

// StopGridPreviews calls drift.v1.GridPreviewService.StopGridPreviews.
func (c *gridPreviewServiceClient) StopGridPreviews(ctx context.Context, req *connect.Request[v1.StopGridPreviewsRequest]) (*connect.Response[v1.StopGridPreviewsResponse], error) {
	return c.stopGridPreviews.CallUnary(ctx, req)
}

// GridPreviewServiceHandler is an implementation of the drift.v1.GridPreviewService service.
type GridPreviewServiceHandler interface {
	// SyncGridPreviews reconciles the plane's capture set with the devices the
	// console is drawing and answers with the still held for each. It is the whole
	// of the grid's traffic: the plane owns the cadence and this call reads what
	// that cadence produced, so a console that polls it does not drive a capture
	// per device and cannot fan out.
	SyncGridPreviews(context.Context, *connect.Request[v1.SyncGridPreviewsRequest]) (*connect.Response[v1.SyncGridPreviewsResponse], error)
	// StopGridPreviews releases the capture set. A device nothing is showing stops
	// being captured, which is the same rule the one-shot capture and the live
	// mirror already obey.
	StopGridPreviews(context.Context, *connect.Request[v1.StopGridPreviewsRequest]) (*connect.Response[v1.StopGridPreviewsResponse], error)
}

// NewGridPreviewServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewGridPreviewServiceHandler(svc GridPreviewServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	gridPreviewServiceMethods := v1.File_drift_v1_grid_preview_proto.Services().ByName("GridPreviewService").Methods()
	gridPreviewServiceSyncGridPreviewsHandler := connect.NewUnaryHandler(
		GridPreviewServiceSyncGridPreviewsProcedure,
		svc.SyncGridPreviews,
		connect.WithSchema(gridPreviewServiceMethods.ByName("SyncGridPreviews")),
		connect.WithHandlerOptions(opts...),
	)
	gridPreviewServiceStopGridPreviewsHandler := connect.NewUnaryHandler(
		GridPreviewServiceStopGridPreviewsProcedure,
		svc.StopGridPreviews,
		connect.WithSchema(gridPreviewServiceMethods.ByName("StopGridPreviews")),
		connect.WithHandlerOptions(opts...),
	)
	return "/drift.v1.GridPreviewService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case GridPreviewServiceSyncGridPreviewsProcedure:
			gridPreviewServiceSyncGridPreviewsHandler.ServeHTTP(w, r)
		case GridPreviewServiceStopGridPreviewsProcedure:
			gridPreviewServiceStopGridPreviewsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedGridPreviewServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedGridPreviewServiceHandler struct{}

func (UnimplementedGridPreviewServiceHandler) SyncGridPreviews(context.Context, *connect.Request[v1.SyncGridPreviewsRequest]) (*connect.Response[v1.SyncGridPreviewsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("drift.v1.GridPreviewService.SyncGridPreviews is not implemented"))
}

func (UnimplementedGridPreviewServiceHandler) StopGridPreviews(context.Context, *connect.Request[v1.StopGridPreviewsRequest]) (*connect.Response[v1.StopGridPreviewsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("drift.v1.GridPreviewService.StopGridPreviews is not implemented"))
}
