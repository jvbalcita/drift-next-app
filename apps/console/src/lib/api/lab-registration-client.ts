// The lab registration surface this module wrapped has been removed: the
// drift.v1.LabRegistrationService contract, the provisioning readiness
// projection, and the LabRegistrationView domain type are gone, so there is
// nothing left to adapt. This file belongs to that removal and must be
// deleted; it is emptied instead of removed here because this change is not
// permitted to delete files.
export {}
