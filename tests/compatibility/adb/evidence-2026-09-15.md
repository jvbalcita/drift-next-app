# P13 live evidence — 2026-09-15

Operator-confirmed target serial: `192.168.1.109:5555`  
Masked correlator (test log): `serial-bf2bd84f`  
Host ADB: `/opt/homebrew/bin/adb` (platform-tools `37.0.1-15733141`)  
Adapter version: `p13.1.0`  
Mode: read-only observation only (enumerate → confirm serial → capture → clear)

## Device facts (allow-listed getprop)

| Property | Value |
| --- | --- |
| `ro.build.version.release` | 12 |
| `ro.build.version.sdk` | 31 |
| `ro.product.model` | SM-G9750 |
| `ro.product.manufacturer` | samsung |
| Connection type | TCP wireless (`host:port`) |
| Transport state | `device` |
| Observed `transport_id` at run | 55 |

## Opt-in test

```text
DRIFT_P13_LAB_MODE=1 \
DRIFT_P13_ADB_PATH=/opt/homebrew/bin/adb \
DRIFT_P13_LAB_SERIAL=192.168.1.109:5555 \
go test ./internal/edge/lab -run TestRealDeviceReadOnlyObservation -v -count=1
```

Result: **PASS** in 12.42s (package ok 13.053s).

Sanitized log excerpt:

```text
discovered 20 candidate transports; none registered or confirmed
target serial-bf2bd84f is enumerated: state=device connection=tcp
serial=serial-bf2bd84f identity=serial-6b47aef7 adapter=p13.1.0 platform-tools=37.0.1-15733141
health=device screenshot=sha256:871cf07f647a86f66bb737a4756425a905fb160e06c2416d34a0fe6a405137d1 bytes=3543647 preview_truncated=false
hierarchy: 7 nodes, depth 2, complete (nodes=7 depth=2 complete=true)
latency_ms=12061 postcondition_verified=true
```

No raw PNG bytes, hierarchy XML, or unmasked serials were committed. Screen content is referenced by SHA-256 only.

## Supplemental host timings (same confirmed serial only)

Measured with `/usr/bin/time -p` against the confirmed serial; not estimated.

| Operation | Result |
| --- | --- |
| `adb get-state` | `device`; real ≈ 0.01s (n=3) |
| `adb shell getprop ro.build.version.sdk` | `31`; real ≈ 0.04–0.06s (n=3) |
| `adb exec-out screencap -p` | PNG 1080×2280 RGBA; 3 543 647 bytes; real 9.80s (host artifact deleted after measure) |
| `adb exec-out uiautomator dump --compressed /dev/tty` | stdout path succeeded; 2 736 bytes XML; real 2.47s |
| Full observation bundle (`LatencyMs`) | 12 061 ms |
| `/sdcard/Android/data` touch+rm probe | `sdcard_ok` |

## Not exercised this run

- Offline / unauthorized / no-permissions transport states
- Real transport-id change → read-only reattach
- USB connection path (lab had zero USB transports)
- Forced timeout / hung-transport indeterminate path
- UIAutomator temporary-file fallback (stdout path succeeded)
- Intentional incomplete-hierarchy postcondition failure on hardware

## Expansion decision

Unchanged: **NO-GO** beyond one confirmed lab device.
